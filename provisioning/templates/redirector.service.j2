[Unit]
Description=HTTP Redirect Server
Wants=network.target
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/{{app_user}}
ExecStart=/usr/bin/authbind /opt/node/bin/node {{webroot}}/index.js 80 443
Restart=always
User={{app_user}}

[Install]
WantedBy=multi-user.target
